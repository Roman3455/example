import { StyleSheet } from 'react-native';
import { colors } from '@/shared/themes/configs/colors';

export const useCustomLinkStyles = StyleSheet.create({
  link: {
    fontFamily: 'FiraSans',
    fontSize: 18,
    color: colors.violetLink,
  },
});
